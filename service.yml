---
apiVersion: v1
kind: Pod
metadata:
  name: k8s-init-service-nginx
  labels:
    purpose: demonstrate-service
    app: nginx
spec:
  containers:
  - name: container-name
    image: nginx:stable
    ports:
      - containerPort: 80